# Dependencias
node_modules/
**/node_modules/
.venv/
venv/
__pycache__/
*.py[cod]

# Builds
.next/
out/
dist/
build/

# Entornos y secretos — NUNCA commitear
.env
.env.*
!.env.example
# Excepción: los .env.production de las apps solo contienen el token PÚBLICO del
# beacon de Cloudflare Web Analytics (ADR-0020), que ya viaja en el JS del navegador.
# No son secretos; se versionan para hornear la analítica en el build. Ver analitica-web.
!apps/*/.env.production
**/env_files/.env.*
**/*service_account*.json
**/*service-account*.json
*.pem
*.key
.venv/

# Firebase / GCP
.firebase/
firebase-debug.log
.firebaserc.local

# Sistema operativo / editores
.DS_Store
Thumbs.db
*.log

# Coverage / tests
.coverage
coverage/
.pytest_cache/

# Misc
*.tmp
.cache/
apps/catalogo/public/catalogo-herpetofauna-chirimoyo.pdf
